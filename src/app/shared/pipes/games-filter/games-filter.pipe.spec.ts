import { GamesFilterPipe } from './games-filter.pipe';

describe('GamesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new GamesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
