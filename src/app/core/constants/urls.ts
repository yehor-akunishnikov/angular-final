const baseUrl = `https://angular-final-project.herokuapp.com`;
const apiAuthUrl = `${baseUrl}/api/auth`;
const invitesUrl = `${baseUrl}/api/users/me/friend-invites`;
const mainAPIUrl = `${baseUrl}/api`;
const friendsUrl = `${baseUrl}/api/users/me/friends`;
const usersUrl = `${baseUrl}/api/users`;
const profileUrl = `${baseUrl}/api/users/me`;
const gamesUrl = `${baseUrl}/api/games`;
const userLibUrl = `${baseUrl}/api/users/me/games`;

export {
  baseUrl,

  apiAuthUrl,
  invitesUrl,
  mainAPIUrl,
  friendsUrl,
  usersUrl,
  profileUrl,
  gamesUrl,
  userLibUrl,
};